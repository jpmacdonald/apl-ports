[package]
strategy = "build"
name = "gdbm"

source_url = "https://git.savannah.gnu.org/git/gdbm.git"
version_pattern = "v{{version}}"
script = """
./configure --prefix=$PREFIX --disable-shared --enable-static
make -j$JOBS
make install
"""
