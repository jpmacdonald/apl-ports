[package]
strategy = "build"
name = "libtool"

source_url = "https://git.savannah.gnu.org/git/libtool.git"
version_pattern = "v{{version}}"
dependencies = ["m4", "autoconf", "automake"]
script = """
./bootstrap
./configure --prefix=$PREFIX
make -j$JOBS
make install
"""
