[package]
strategy = "build"
name = "expat"

source_url = "https://github.com/libexpat/libexpat"
version_pattern = "R_{{version}}"
dependencies = ["autoconf", "automake"]
script = """
cd expat
./buildconf.sh
./configure --prefix=$PREFIX \
    --without-docbook
make -j$JOBS
make install
"""
