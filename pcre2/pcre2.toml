[package]
strategy = "build"
name = "pcre2"

source_url = "https://github.com/PCRE2Project/pcre2"
version_pattern = "pcre2-{{version}}"
script = """
./autogen.sh
./configure --prefix=$PREFIX \
    --disable-shared \
    --enable-static \
    --enable-jit
make -j$JOBS
make install
"""
