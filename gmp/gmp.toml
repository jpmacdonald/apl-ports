[package]
strategy = "build"
name = "gmp"

source_url = "https://github.com/alisw/GMP"
version_pattern = "v{{version}}"
script = """
./configure --prefix=$PREFIX \
    --disable-shared \
    --enable-static \
    --enable-cxx
make -j$JOBS
make install
"""
